{
  "name": "drone-example",
  "version": "1.0.0",
  "description": "system to track the location of every drone in real-time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "richardmarais",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "morgan": "^1.9.1",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "eslint": "^6.1.0"
  }
}
